# Mono Chrome QR

Professional offline-first QR code app built with Expo and React Native.

## Android APK

This project is configured for Expo Application Services (EAS).

Build an installable Android APK with:

```bash
npx eas-cli@latest login
npx eas-cli@latest build --platform android --profile preview
```

The `preview` profile produces an `.apk` file.
